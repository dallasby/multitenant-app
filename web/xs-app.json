{
  "authenticationMethod": "route",
  "routes": [
    {
      "source": "^/multitenant-app/(.*)$",
      "target": "$1",
      "destination": "multitenant-app",
      "authenticationType": "xsuaa"
    }
  ]
}
